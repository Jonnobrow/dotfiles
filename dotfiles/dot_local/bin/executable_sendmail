#!/bin/sh
input=`cat`
echo "$input" | /usr/bin/msmtp -C $HOME/.config/msmtp/msmtprc \
    --read-envelope-from --read-recipients -a $1  \
    && echo "$input" | notmuch insert --folder $1/Sent +sent \
    || echo "Failed to send mail"
